# MerchantShield AI — Python dependencies
# Pinned to the versions this project was built and tested against.
# Install with: pip install -r requirements.txt
# (If pip complains about an externally-managed environment, add --break-system-packages.)

# --- ML / data pipeline (Phases 1-7) ---
numpy==2.4.4
pandas==3.0.2
scikit-learn==1.8.0
lightgbm==4.7.0
shap==0.52.0
joblib==1.5.3
matplotlib==3.10.8

# --- Backend API (Phase 8) ---
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.4
SQLAlchemy==2.0.52

# --- Testing (all phases) ---
pytest==9.1.1
httpx==0.28.1
