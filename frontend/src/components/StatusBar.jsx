// StatusBar is no longer used as a separate component — status information
// has been integrated into the redesigned App.jsx header.
// This stub is retained so any future import does not break.
export default function StatusBar() {
  return null;
}
